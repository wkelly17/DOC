[pytest]
# addopts = --tb=long
# -x is stop at first test failure and --pdb is on failure start pdb
# at the line of failure
addopts = --tb=short -x --pdb --log-file=tests_output.log --log-cli-level=DEBUG
filterwarnings =
    ignore::DeprecationWarning
